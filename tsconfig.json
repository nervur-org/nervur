{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // A suite names the package by its entries, as a stranger does, and
    // reads the source each entry is built from.
    "customConditions": ["nervur-source"]
  },
  "include": ["src", "test", "deep"]
}
