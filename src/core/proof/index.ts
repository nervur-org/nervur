// SPDX-License-Identifier: Apache-2.0
// `nervur/proof`: the suites a body of a stranger's passes to be supported,
// the kit's own. Each is a list of scenes, a title and a function, run by
// whatever runner the stranger keeps, in the engine the body is for:
//
//   for (const [title, scene] of memoryScenes('S3Memory', make, expect, half)) test(title, scene);
//
// `expect` throws on the first assertion that fails. The law's scenes stand
// harbors on a stranger's bodies and hold the unpacking law over them, and
// the keeper's scenes hold a being's fields over the root memory and over
// every memory faculty.
export { entropyScenes, custodyScenes, loaderScenes, memoryScenes, type Cut, type Scene, type Source } from './contracts.ts';
export { expect, type Expect } from './expect.ts';
export { keeperScenes, LAW, law, lawBundle, lawScenes, type Bodies, type Terrain } from './law.ts';
