// The shop's house module, served on the origin at `/houses/shop.js` for the
// origin body to load, sharing the page's one `nervur/being`.
export { beings, steward } from '../../../test/fixtures/world/shop.ts';
