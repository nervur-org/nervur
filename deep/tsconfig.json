{
  // The terrains' runs, checked when `deep` runs and read by the linter,
  // with the tools the repository's root carries beside the package:
  // Playwright and esbuild.
  "extends": "../tsconfig.json",
  "include": ["../src", "../test", "."]
}
